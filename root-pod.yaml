apiVersion: v1
kind: Pod
metadata:
  name: root-pod
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx
    securityContext:
      runAsUser: 0

### Create a Pod named root-pod in default namespace that runs the nginx image with root privileges.

## Ensure that the securityContext is configured correctly to run as a root user.